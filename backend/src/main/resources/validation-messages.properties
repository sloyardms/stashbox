# VALIDATION MESSAGES - Bean Validation

# ==============================================================================
# Commmon validation messages
# ==============================================================================

common.update.atLeastOneFieldProvided=At least one field must be provided for update

# ==============================================================================
# User validation messages
# ==============================================================================

user.externalId.notNull=External User ID is required

user.userName.notBlank=Username is required
user.userName.maxSize=Username must not exceed {max} characters

user.update.atLeastOneFieldProvided=At least one field must be provided for update


# ==============================================================================
# UserFilter validation messages
# ==============================================================================

userFilter.filterName.notBlank=Filter name is required
userFilter.filterName.maxSize=Filter name must not exceed {max} characters

userFilter.urlPattern.notBlank=Url pattern is required
userFilter.urlPattern.maxSize=Url pattern must not exceed {max} characters

userFilter.extractionRegex.notBlank=Extraction regex is required
userFilter.extractionRegex.maxSize=Extraction regex must not exceed {max} characters

userFilter.update.atLeastOneFieldProvided=At least one field must be provided for update

# ==============================================================================
# ItemGroup validation messages
# ==============================================================================

itemGroup.name.notBlank=Name is required
itemGroup.name.maxSize=Name must not exceed {max} characters

itemGroup.description.maxSize=Description must not exceed {max} characters

itemGroup.update.atLeastOneFieldProvided=At least one field must be provided for update